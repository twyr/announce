{
	"hooks": {
		"commit-msg": "npx commitlint -E HUSKY_GIT_PARAMS",
		"pre-commit": "npx lint-staged --allow-empty",
		"pre-push": "npx lint-staged --allow-empty",
		"prepare-commit-msg": "exec < /dev/tty && git cz --hook || true",
	}
}
